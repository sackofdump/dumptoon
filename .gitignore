# Editor / OS
.DS_Store
Thumbs.db
*.swp

# Node (in case you add a build step)
node_modules/
npm-debug.log*

# Secrets — Supabase / API keys go in config.local.js (never commit)
.env
.env.*
config.local.js

# Claude Code per-project settings (local permissions allowlist, etc.)
.claude/

# Asset extraction debug overlay (regenerable from sheet + script)
Assets/_debug_bounds.png
